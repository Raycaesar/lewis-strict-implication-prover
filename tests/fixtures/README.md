# Test Fixtures

This directory is for small machine-readable fixtures used by kernel and parser tests.

Future fixture categories:

```text
valid-formulas/
invalid-formulas/
valid-certificates/
invalid-certificates/
schema-instances/
substitutions/
replacement-paths/
```

Keep fixtures minimal. Historical full proofs belong in `tests/historical/` or `proofs/`, not here.
